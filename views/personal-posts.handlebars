
 
          {{#if logged_in}}
 <div class="container mt-40 mx-auto" style="min-height: 67vh;">

    <div class="w-full px-4 shadow">
{{#each posts as |post|}}
        <a div class="flex flex-row justify-between bg-white w-full mb-8 shadow-lg rounded-lg p-5" href="/dashboard/edit/{{post.id}}">
            <span class="text-xl font-semibold">{{post.tittle}}</span>
            <span>{{post.createdAt}}</span>
        </a>


        </div>
        </div>
          {{/each}}
        <div class="text-center">
            <a href="/dashboard/new"><button class="bg-gray-900 text-white active:bg-gray-700 text-sm font-bold uppercase px-6 py-3 rounded shadow hover:shadow-lg outline-none focus:outline-none mr-1 mb-1" type="button">
              ➕ New Post
  </button></a>
        </div>
         {{/if}}
        </div>





