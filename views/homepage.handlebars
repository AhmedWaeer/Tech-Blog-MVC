
  {{#each posts as |post|}}
<div class="w-full px-4 shadow">
    <div class="flex flex-col min-w-0 break-words bg-white w-full mb-8 shadow-lg rounded-lg">
        <div class="px-4 py-10 flex-auto">
            <div class="text-white p-3 text-center inline-flex items-center justify-center w-12 h-12 mb-5 shadow-lg rounded-full bg-blue-400">
                <i class=""></i>
            </div>
             <input type="hidden" name="post-id" value="{{post.id}}" />
            <a href="/post/{{post.id}}"><h6 class="text-xl font-semibold">{{post.tittle}} <span class="text-gray-600">created by {{user.email}} on {{post.createdAt}}</span></h6></a>
            <p class="mt-2 mb-4 text-gray-600">
                {{post.body}}
            </p>
        </div>
        </div>
        </div>
     {{/each}}  






    